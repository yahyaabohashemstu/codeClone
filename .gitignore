*.env
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
env/
venv/
ENV/
.coverage
.pytest_cache/
htmlcov/
.idea/
.vscode/
*.swp
*.swo
.vs/
node_modules/
*.db
logs/
*.log
.history/
.mypy_cache/
SourcererCC/
LinkedDataHub/
vendor/
chrome*/
instance/
!instance/.gitkeep


#Ignore vscode AI rules
.github/instructions/codacy.instructions.md

# Archive files
*.rar

# Temporary output files
tmp_app_stderr.txt
tmp_app_stdout.txt

# PDF reports generated at runtime
*.pdf

# Uploads directory (user-submitted files)
uploads/

# Backup files
*.backup
app.py.react_migration_backup

# Secret key material saved locally — MUST NOT be committed
ENTERPRISE_DATA_KEY.txt
*.key
secret_key

# Evaluation run artifacts (regenerable via evaluation/run_eval.py); keep report.md
evaluation/results/metrics.json
evaluation/results/full_run.log

# Frontend test artifacts
code-sleuth-react-ui/coverage/
code-sleuth-react-ui/test-results/
code-sleuth-react-ui/playwright-report/
code-sleuth-react-ui/.playwright/

# TypeScript incremental build cache (regenerable)
*.tsbuildinfo

# Claude Code local settings (may contain sensitive paths)
.claude/

# Local working materials (raw font downloads + design-spec kit), not runtime code
fonts/
new design/

# Stripe CLI binary (downloaded locally, not part of the repo)
stripe.exe
stripe_cli.zip
